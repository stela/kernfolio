spring:
  datasource:
    url: jdbc:h2:mem:kernfolio-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  liquibase:
    enabled: false

market-data:
  scheduler:
    enabled: false
  default-currencies:
    - USD
    - CAD
