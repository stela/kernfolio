# Vault root token for production deployments. The docker-compose.yml
# web service reads this as ${VAULT_APP_TOKEN:-dev-root-token}; the
# default only works for local dev (which runs Vault in -dev mode via
# docker-compose.dev.yml). Generate a real token from your production
# Vault instance.
VAULT_APP_TOKEN=changeme
